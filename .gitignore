# ---- PRIVATE: design docs and prototype never leave this machine ----
docs-private/
prototype/

# ---- dependencies / build ----
node_modules/
dist/
# The older client, extracted by scripts/older-client.mjs (5.2).
.older-client/
# Local tool state: editor launch settings, and agents' worktrees.
.claude/
build/
.turbo/
*.tsbuildinfo
coverage/
playwright-report/
test-results/
.playwright/

# ---- the phone app lives in its own private repository ----
# Never here: its native projects, its build output, or anything that signs it.
android/
ios/
.expo/
*.jks
*.keystore
*.p12
*.p8
google-services.json
GoogleService-Info.plist

# ---- environment / secrets ----
.env
.env.*
!.env.example
*.pem
*.key
# The worker's fake push distributor (tests only, on 127.0.0.1): a throwaway pair.
!apps/worker/src/jobs/testdata/push-tls.key
data/

# ---- editor / OS ----
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.log
